name: Build Debug APK

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    name: Build Debug APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Remove local Java path from gradle.properties
        run: sed -i '/org.gradle.java.home/d' gradle.properties

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Inject Google Services JSON and local.properties
        env:
          GOOGLE_SERVICES_JSON: ${{ secrets.GOOGLE_SERVICES_JSON }}
          GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
          STATS_API_KEY: ${{ secrets.STATS_API_KEY }}
          STATS_BASE_URL: ${{ secrets.STATS_BASE_URL }}
          AUTH_API_BASE_URL: ${{ secrets.AUTH_API_BASE_URL }}
        run: |
          if [ -z "${GOOGLE_SERVICES_JSON}" ]; then
            cat <<EOF > app/google-services.json
          {
            "project_info": { "project_number": "1234567890", "project_id": "dummy-project" },
            "client": [ 
              { "client_info": { "mobilesdk_app_id": "1:1234567890:android:abcdef123456", "android_client_info": { "package_name": "com.darkxvenom.airbeats786" } }, "api_key": [ { "current_key": "dummy-key" } ] },
              { "client_info": { "mobilesdk_app_id": "1:1234567890:android:abcdef123456", "android_client_info": { "package_name": "com.darkxvenom.airbeats786.debug" } }, "api_key": [ { "current_key": "dummy-key" } ] }
            ],
            "configuration_version": "1"
          }
          EOF
          else
            echo "${GOOGLE_SERVICES_JSON}" | base64 --decode > app/google-services.json
          fi
          
          touch local.properties

      - name: Build Signed APKs
        run: ./gradlew assembleRelease assembleDebug

      - name: Upload Release APK
        uses: actions/upload-artifact@v4
        with:
          name: AirBeats-Release-APK
          path: app/build/outputs/apk/release/*.apk

      - name: Upload Debug APK
        uses: actions/upload-artifact@v4
        with:
          name: AirBeats-Debug-APK
          path: app/build/outputs/apk/debug/*.apk
